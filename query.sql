CREATE TABLE event_login(
el_id INT,
el_user VARCHAR(30),
el_pwd VARCHAR(30),
PRIMARY KEY (el_id)
);

CREATE TABLE tenant_login(
tl_id INT,
tl_user VARCHAR(30),
tl_pwd VARCHAR(30),
PRIMARY KEY (tl_id)
);

CREATE TABLE 
